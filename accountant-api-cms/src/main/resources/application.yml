spring:
  application:
    name: accountant-api-cms

  main:
    web-application-type: servlet  # Явно указываем тип приложения
    banner-mode: console

  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
        maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true # Показывать SQL в логах

  devtools:
    restart:
      enabled: true

server:
  port: 8080
  servlet:
    context-path: /

# Настройки rate limiter
rate-limiter:
  max-requests: 100
  time-window-seconds: 60
  map-name: rate-limiter-store
  time-to-live-seconds: 120
  max-idle-seconds: 60

# Swagger
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true